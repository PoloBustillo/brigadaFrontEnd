# Copy to .env.local and fill in your values.
# All EXPO_PUBLIC_* vars are embedded into the bundle at build time.

# Sentry DSN — create a project at https://sentry.io to get this value.
# Leave empty during local development (Sentry is disabled when NODE_ENV != production).
EXPO_PUBLIC_SENTRY_DSN=

# Sentry auth token — required to upload source maps on EAS builds.
# Generate at https://sentry.io/settings/account/api/auth-tokens/
# Add to EAS as a secret:  eas secret:create --scope project --name SENTRY_AUTH_TOKEN --value <token>
# Then set "uploadSourceMaps": true in app.json Sentry plugin config.
SENTRY_AUTH_TOKEN=
